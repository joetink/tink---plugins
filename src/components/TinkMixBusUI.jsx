import React from 'react'
export default function TinkMixBusUI(props){ return <div style={{color:'#fff'}}>Mix Bus UI is in artifact preview - integrate same as Channel. See tink_mix_bus_strip_agentic_artifact</div> }
